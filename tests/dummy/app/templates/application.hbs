<h1 id="title">Welcome to Ember Simple Auth for Auth0</h1>
<header>
  {{#if session.isAuthenticated}}
    <h3>You are currently logged as: {{session.data.authenticated.profile.email}}</h3>
    <a href="#" {{action "logout"}}>Logout</a>
  {{/if}}
</header>

{{outlet}}