<h3>
  You are currently logged as: {{session.data.authenticated.profile.email}}
  <img src="{{session.data.authenticated.profile.picture}}" width="50" height="50">
</h3>
<h3>
  {{#if session.data.authenticated.profile.impersonated}}
    Impersonated by: {{session.data.authenticated.profile.impersonator.email}}
  {{/if}}
</h3>
<div>
  Token expires {{moment-calendar expiresIn}} {{moment-from-now expiresIn}}
</div>
<a href="#" {{action "logout"}}>
  {{#if session.data.authenticated.profile.impersonated}}
    Stop Impersonating
  {{else}}
    Logout of Ember Simple Auth
  {{/if}}
</a>
<br/>
<a href="#" {{action "logoutOfAuth0"}}>
  Logout of Auth0 Session
</a>

<h2>Example of protected route that requires the user to be authenticated.</h2>
<h3>Secret code is 12345</h3>